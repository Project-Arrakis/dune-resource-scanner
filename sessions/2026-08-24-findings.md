# 2026-08-24 — #16 root cause; two defects fixed

Point-in-time record. The living account is `CONTINUATION.md` (Session 4 section);
the full evidence is in `findings/2026-08-24-issue-16/`.

## Environment as found

- `dune-dev` up, `DeepDesert_1` PID **390735**, `Survival_1` PID **377758**.
- Coriolis seed still **2** for both maps — unchanged since session 3, so Deep
  Desert had **not** regenerated and `dune.markers` was valid ground truth.
- No orphaned scan processes from previous sessions.

## What was done

1. **Reproduced #16** against fresh ground truth: 211 markers in the WindPass box,
   baseline scan returns 96 actors at only 42 distinct positions, 6 on a marker.
2. **Instrumented the funnel** — pass 1 finds **211/211 (100%)**, pass 2 yields
   **6/211 (2.8%)**. The loss is entirely in pass 2.
3. **Disproved #16's own hypothesis** with an offset-agnostic probe: nothing points
   into the 2 KB before these nodes' transforms, at any region setting.
4. **Identified what the objects are** — 384-byte spawn records in an array, with a
   pre-trace and a terrain-snapped position triple, not UObject actors.
5. **Found a 25× better resolution path** — filtering on the spawn-record signature
   gives 152/211 (72%) at 1–2 copies per marker instead of 2.8% and 8–334 copies.
6. **Fixed #14** (PR #17) and **found and fixed #18** (PR #19).

## Two process notes worth carrying forward

- **A local `timeout ssh` still does not kill the remote process.** A diagnostic
  with an unbounded working set was building a multi-billion-entry map on a host
  with 24 GB free that also runs the live game server. It was caught and killed
  manually before any OOM; game PIDs and free memory were re-verified afterwards.
  Bound the working set *inside* the tool, not just with a wall-clock timeout —
  `diagtype` now refuses to run its offset probe above a hit threshold for exactly
  this reason.
- **A scoring metric can look like a result.** The first type-discriminator search
  reported `pure = 15` — an artifact of `0x0` and a shared vtable satisfying "every
  marker of this type shares a value". Both the wrong metric and the corrected one
  are kept in `findings/`, because the wrong output is plausible enough to be
  believed by the next reader.

## Still open

- **#16 remains open** — root cause established, fix is a redesign, not written.
- Empty scan still emits `null` rather than `[]`.
- PRs #17 and #19 are green on all four required checks but **not merged** — the
  session's tooling declined the merge action, so they need an operator.
